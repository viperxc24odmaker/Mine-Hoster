# UI stability pass

Shared UI modules added for the redesign: `ui_theme.py`, `ui_components.py`, `ui_actions.py`, and `ui_audit.py`. Marketplace helpers live in `plugin_utils.py` and `plugin_marketplace_ui.py`. Destructive reset/delete logic is centralized in `ui_safety.py` and `reset_plan.py`.
